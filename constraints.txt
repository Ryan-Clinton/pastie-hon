# Every package Pastie installs, pinned to the exact version it was last tested
# with - the direct dependencies *and* everything they pull in.
#
#     pip install -e ".[dev]" -c constraints.txt
#
# pyproject.toml pins the direct dependencies, and that turned out not to be
# enough. On 2026-09-11 CI resolved multidict 6.8.0 where the machine that had
# generated the licence notices held 6.7.1, and the notices check failed on a
# commit that changed no dependency at all. "Two people cloning a month apart
# get the same software" is only true with this file.
#
# Regenerate from a clean virtual environment on Windows, never from a working
# one - a working environment accumulates things nobody asked for:
#
#     python -m venv fresh
#     fresh\Scripts\pip install -e ".[dev]"
#     fresh\Scripts\pip freeze --exclude-editable      (paste below this header)
#
# Windows-only packages here (pywin32, pefile, colorama) are simply ignored on
# Linux: pip applies a constraint only to a package something actually needs.
aiohappyeyeballs==2.7.1
aiohttp==3.14.3
aiosignal==1.4.0
altgraph==0.17.5
ast_serialize==0.11.1
attrs==26.1.0
awscrt==0.36.1
awsiotsdk==1.31.0
casttube==0.2.1
certifi==2026.7.22
charset-normalizer==3.5.1
click==8.1.8
colorama==0.4.6
coverage==7.16.0
frozenlist==1.8.0
gTTS==2.5.4
idna==3.19
ifaddr==0.2.0
iniconfig==2.3.0
librt==0.15.0
multidict==6.8.0
mypy==2.3.1
mypy_extensions==1.1.0
packaging==26.3
pathspec==1.1.1
pefile==2024.8.26
pillow==11.3.0
pip-licenses==5.0.0
pluggy==1.6.0
prettytable==3.18.0
propcache==0.5.2
protobuf==7.36.1
PyChromecast==14.0.7
Pygments==2.21.0
pyhon-revived==0.19.2
pyinstaller==6.22.2
pyinstaller-hooks-contrib==2026.7
pytest==8.4.2
pytest-asyncio==1.2.0
pytest-cov==7.0.0
pywin32==312
pywin32-ctypes==0.2.3
requests==2.34.2
ruff==0.16.6
setuptools==84.0.0
tomli==2.4.1
typing_extensions==4.16.0
urllib3==2.7.0
wcwidth==0.8.3
yarl==1.24.5
zeroconf==0.151.3
